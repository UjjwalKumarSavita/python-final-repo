# Later we’ll add DATABASE_URL for Postgres/pgvector; for now, file storage only
APP_ENV=dev
API_HOST=127.0.0.2
API_PORT=8000
CORS_ORIGINS=http://localhost:8501

# Optional (future milestones)
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=intelligent-docs


# Optional: Postgres + pgvector
DATABASE_URL=

# Optional: LLM for summarization (fallback used if not set)
LLM_PROVIDER=
OPENAI_API_KEY=


# ...
SUMMARY_WORDS_DEFAULT=350   # default target summary length in words
