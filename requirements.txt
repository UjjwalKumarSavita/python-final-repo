# Core backend
fastapi
uvicorn
pydantic
pydantic-settings
python-dotenv
requests
python-multipart
pytest

# UI
streamlit

# Parsing
pypdf
PyPDF2
python-docx
beautifulsoup4
lxml   # optional but recommended


# Lightweight numeric ops (embeddings + similarity)
numpy

# Optional: Postgres + pgvector (comment out if not using DB now)
psycopg[binary]>=3.2
psycopg[binary]
pgvector

pydantic
pydantic-settings
cryptography

# --- AutoGen AgentChat (current MS library) ---
autogen-agentchat>=0.7.4,<0.8
autogen-ext[openai]>=0.7.4,<0.8


# add this for PDF export
reportlab